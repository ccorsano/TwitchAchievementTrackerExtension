import * as React from 'react';
import * as Base from '../../common/ConfigStepBase'

export default class ConfigSteam_02_AppId extends Base.ConfigStepBase<any, any> {
    constructor(props: any){
        super(props);
    }
}